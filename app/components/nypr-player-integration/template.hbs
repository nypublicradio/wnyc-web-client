{{yield (hash
  currentTitle          = currentTitle

  showTitle             = showTitle
  showUrl               = showUrl
  storyTitle            = storyTitle
  storyUrl              = storyUrl
  songDetails           = songDetails

  currentAudio          = currentAudio
  story                 = story

  isStream              = isStream
  streamScheduleUrl     = streamScheduleUrl
  streamPlaylistUrl     = streamPlaylistUrl
  streamUrl             = streamUrl
  streamName            = streamName

  backdropImageUrl      = backdropImageUrl

  onPlay                = (action 'onPlay')
  onPause               = (action 'onPause')
  onFastForward         = (action 'onFastForward')
  onRewind              = (action 'onRewind')
  onSetPosition         = (action 'onSetPosition')
  onDismissNotification = (action 'onDismissNotification')

  revealNotificationBar = revealNotificationBar  
)}}
