{{#with model.story as |s|}}
  {{#nypr-ui/hero as |hero|}}
    {{#with s.imageMain as |img|}}
      {{hero.image
        src=(image-template img.template 1280 0 img.crop)
        source=img.source
        caption=img.caption
      }}
    {{/with}}
    {{#hero.top as |top|}}
      {{#top.left}}
        {{#link-to 'index' class="hero-link"}}
          {{nypr-svg icon="left-arrow" className="hero-back"}}
          {{nypr-svg icon="wnyc-logo" className="hero-logo"}}
        {{/link-to}}
      {{/top.left}}
      
      {{#top.right}}
        {{#nypr-ui/buttons/red tagName="a" href="http://www.wnyc.org" tall=true}}
          Donate
        {{/nypr-ui/buttons/red}}
      {{/top.right}}
    {{/hero.top}}
    
    {{#hero.headline}}
      {{s.title}}
    {{/hero.headline}}
    
    {{#hero.blurb}}
      <p>
        {{{s.tease}}}
      </p>
      
      {{#if (and s.audioMayStream s.audio)}}
        {{#listen-button
          type='white-hollow-block'
          itemPK=s.slug
          itemTitle=s.title
          itemShow=(or s.showTitle s.headers.brand.title)}}
          Listen to the story
        {{/listen-button}}
      {{/if}}
      
    {{/hero.blurb}}
    
    {{#hero.lockup}}
      social buttons
    {{/hero.lockup}}
  {{/nypr-ui/hero}}
{{/with}}
