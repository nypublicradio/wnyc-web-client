<header class="discover-setup-header">
  <div class="discover-setup-header-action">

    {{link-to "Back" 'discover.start' class="rounded-caps-button"}}

  </div>
  <div>
    <h2 class="discover-setup-title-step h2 text--white">Pick Your Topics</h2>
  </div>
  <div class="discover-setup-header-action">
    {{#if hasNotSelectedATopic}}
      <button class="rounded-caps-button" disabled='disabled'>Next</button>
    {{else}}
      <button class="rounded-caps-button mod-filled-red" disabled={{hasNotSelectedATopic}} {{action 'next' currentTopicSelection}}>Next</button>
    {{/if}}
  </div>
</header>

{{discover-topic-list
  topics=model.topics
  selectedTopics=model.savedTopics
  onNoneSelected=(action (mut hasNotSelectedATopic))
  onTopicsUpdated=(action (mut currentTopicSelection))}}

{{outlet}}
